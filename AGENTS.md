# Agent Instructions

- After adding a feature, commit the change and push it to the remote branch.
- On Windows, run pytest outside the sandbox/escalated in this repo; sandboxed pytest runs can hang.
